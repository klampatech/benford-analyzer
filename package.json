{
  "name": "benford_analyzer",
  "version": "1.0.0",
  "description": "Benford's Law analysis API",
  "type": "module",
  "scripts": {
    "dev": "uvicorn src.api.main:app --reload",
    "start": "uvicorn src.api.main:app --host 0.0.0.0 --port 8000",
    "test": "python -m pytest tests/ -v",
    "lint": "ruff check src/"
  },
  "dependencies": {
    "fastapi": "^0.109.0",
    "uvicorn": "^0.27.0",
    "pydantic": "^2.5.0",
    "httpx": "^0.26.0",
    "beautifulsoup4": "^4.12.0",
    "benford-analyzer-core": "file:./src/core"
  }
}
